<?php
session_start();

// Clear specific guide session data
unset($_SESSION['guide_id']);
unset($_SESSION['guide_name']);
unset($_SESSION['role']);

// Wipe and Destroy
$_SESSION = array();
session_destroy();

// FIXED PATH: Go UP one level to find the login page in the guide/ folder
header("Location:guide_login.php?status=loggedout");
exit();
?>